import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center mt-52'>
            <button class="btn btn-error text-white font-bold loading">loading</button>
        </div>
    );
};

export default Loading;